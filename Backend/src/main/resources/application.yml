spring:
  application:
    name: Backends
  datasource:
    url: jdbc:postgresql://localhost:5432/Learning
    username: postgres
    password: 071208
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
